# Frontend (starter)

Minimal React frontend using CDN-served React (no build) for quick local testing.

It fetches articles from `/articles` and can trigger `/generate`.

Run locally using the included Dockerfile or `docker-compose`.
